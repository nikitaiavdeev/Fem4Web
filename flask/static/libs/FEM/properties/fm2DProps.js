class fmPShell extends fmProps{
	constructor(card) {
        super()
		this.t = t; //Thickness
		this.mid = mid; //Material ID
	}
}

class fmPShear extends fmProps{
	constructor(card) {
		this.t = t; //Thickness
		this.mid = mid; //Material ID
	}
}