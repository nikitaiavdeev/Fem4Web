class fmCBAR extends fmElms {
    constructor(card) {
        super(card[1], card[2],
            [
                card[3],
                card[4]
            ]);
    }
}

class fmCBEAM extends fmElms {
    constructor(card) {
        super(card[1], card[2],
            [
                card[3],
                card[4]
            ]);
    }
}

class fmCROD extends fmElms {
    constructor(card) {
        super(card[1], card[2],
            [
                card[3],
                card[4]
            ]);
    }
}